DB_USERNAME="peppermint"
DB_PASSWORD="1234"
DB_HOST=localhost
DATABASE_URL="postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}/peppermint"
SECRET="supersecret"

# Comma-separated list of allowed origins for CORS (e.g. https://app.example.com,https://admin.example.com).
# If not set, the API will echo back the request Origin header (safe for same-origin deployments).
CORS_ORIGIN=""

